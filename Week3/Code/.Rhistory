cd
m=matrix(data, nrow = rows, ncol = cols)
m=matrix(data, nrow = rows, ncol = cols)
a=2
a
a=(1,2,3,2,)
a=(1,2,3,2)
a=91,2,3,2)
a=（1,2,3,2）
a=(1,2)
a=as.matrix(1,2,3)
a
v = c(1,2,3,4)
v
rm(v)
# This function calculates heights of trees given distance of each tree
# from its base and angle to its top, using  the trigonometric formula
#
# height = distance * tan(radians)
#
# ARGUMENTS
# degrees:   The angle of elevation of tree
# distance:  The distance from base of tree (e.g., meters)
#
# OUTPUT
# The heights of the tree, same units as "distance"
TreeHeight <- function(degrees, distance){
radians <- degrees * pi / 180
height <- distance * tan(radians)
print(paste("Tree height is:", height))
return (height)
}
MyData <- read.csv("../data/trees.csv", header = TRUE) # import with headers
getwd()
setwd("/Users/tisssu/Documents/CMEECourseWork/Week3/Code")
setwd("/Users/tisssu/Documents/CMEECourseWork/Week3/Code")
getwd()
# This function calculates heights of trees given distance of each tree
# from its base and angle to its top, using  the trigonometric formula
#
# height = distance * tan(radians)
#
# ARGUMENTS
# degrees:   The angle of elevation of tree
# distance:  The distance from base of tree (e.g., meters)
#
# OUTPUT
# The heights of the tree, same units as "distance"
TreeHeight <- function(degrees, distance){
radians <- degrees * pi / 180
height <- distance * tan(radians)
print(paste("Tree height is:", height))
return (height)
}
MyData <- read.csv("../data/trees.csv", header = TRUE) # import with headers
View(MyData)
View(MyData)
MyData
Species
class(MyData$Species)
MyData$Species
class(MyData$Distance.m)
?array
Tree.Height.m = array(0,dim(MyData[1]))
Tree.Height.m[112]
for (i in 1:dim(MyData)[1])
{
Tree.Height.m[i]=TreeHeight(MyData[i,3],MyData[i,2])
}
MyData=cbind(MyData,Tree.Height.m)
View(MyData)
rm(list =ls())
source("TreeHeight.R")
getwd()
write.csv(MyData, "../results/TreeHts.csv", row.names=TRUE) # write row names
write.csv(MyData, "../results/TreeHts.csv") # write row names
write.csv(MyData, "../results/TreeHts.csv",row.names = FALSE) # write row names
rm(list=ls())
